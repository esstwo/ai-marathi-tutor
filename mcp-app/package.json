{
  "name": "marathi-mitra-mcp-app",
  "version": "1.0.0",
  "description": "MCP App — interactive Marathi tutor UI inside Claude",
  "type": "module",
  "scripts": {
    "build:apps": "VITE_APP=progress vite build && VITE_APP=conversation vite build && VITE_APP=lessons vite build",
    "build:server": "tsc -p tsconfig.json",
    "build": "npm run build:apps && npm run build:server",
    "start": "node dist/server.js",
    "dev": "tsx server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "@supabase/supabase-js": "^2.105.4",
    "cors": "^2.8.5",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vite-plugin-singlefile": "^2.0.0"
  }
}
